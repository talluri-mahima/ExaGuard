# ExaGuard engineering

This is a separate hackathon repository, not the parent AETHER product.
Use seeded synthetic data with provenance. Never use labels as detector features.
Money uses Decimal or integer cents. Exasol is the application data platform;
the local shadow engine is only for tests and explicit benchmark comparisons.
Keep Streamlit and the service separated by /api/v1 HTTP endpoints.
Persist evidence and deterministic hashes. Never equate a pattern to proven crime.
Measure KPIs; explicitly disclose truncated searches and unverified integrations.
Do not commit credentials, generated bulk data, or assistant coauthor trailers.
